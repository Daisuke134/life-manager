# A15 Foundation handover

正本spec: `/Users/anicca/Projects/life-manager-main/.worktrees/a15-current-release-fence-20260917/docs/superpowers/specs/2026-09-15-life-manager-agent-architecture-refinement.md` の先頭 `Active A15 foundation cursor — atomic remaining TODO`。A15-01からA15-14が唯一の現在チェックリストで、下の古いA15記録は履歴。

実装worktree: `/Users/anicca/Projects/life-manager-main/.worktrees/a15-current-release-fence-20260917`。branch/upstream: `fix/lm-a15-plist-recovery-plan-20260917` / `origin/fix/lm-a15-plist-recovery-plan-20260917`。確認済みHEAD `d80f434804`、当時clean、当時の`origin/main` `462180054d`。開始時にfresh fetch、HEAD/upstream/dirty/source-boundaryを再確認する。共有checkoutと別ownerのworktree・Browser・private stateは編集しない。

完了済み: main-only global `current` fence PR #5351。owner-turn fairness PR #5354。main由来releaseで `x402-ledger` → `x402-experiment-franklin1` → `founder-loop-cadence` → `x402-inflow-watch` の異なるownerの自然terminalを観測。`lm-loop doctor` は観測時 `ok=true`、unmanaged/missing/retired 0。ただし全Foundation Doneではない。

現在の未完コード: `runtime/loop/lm_loop_apply.py` と `runtime/loop/tests/test_lm_loop_apply.py` の同一owner環境JSON→正規plist復旧を上記branchにpush済み。修正前RED、修正後pre-rebase 91 tests +30 subtests PASS。post-rebase再試験は中断、PR未作成、main未統合、本番未反映。別ownerの環境JSONは拒否する。旧branch `fix/lm-a15-env-plist-recovery-20260917` はrebase前の先行版でありforce-pushしない。

本番の読み取り専用最終観測: release-reconcilerのloaded SHAは`a40707143c`で自然terminal `pass` 後も次runがrunning。`writer-report`のinstalled plistは環境JSONでありXMLではない。Writerの`effect_unknown`は未解決で、送信/投稿を再試行・フェンス解除しない。Writer provider処理は別Codexの担当。cleanerのmain由来自然`pass`は観測済みだがdisk空きは約2 GiBで変動する。状態は変わるため再読する。

最初の安全な一手: 現worktreeで `python3 -m pytest -q runtime/loop/tests/test_lm_loop_apply.py` と `git diff --check` をfresh main上で再実行する。PASS後、正本A15-02へ進む。A15をDoneにするのはspecのA15-01..14と自然run/receiptが揃った時だけ。loaded/running/exit 0やmockを外部成果とみなさない。
