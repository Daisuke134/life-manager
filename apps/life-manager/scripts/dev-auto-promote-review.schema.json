{
  "type": "object",
  "additionalProperties": false,
  "required": ["status", "reviewed_head", "reviewed_tree_sha256", "blocking_findings", "evidence"],
  "properties": {
    "status": {
      "type": "string",
      "enum": ["pass", "fail"]
    },
    "reviewed_head": {
      "type": "string",
      "pattern": "^[a-f0-9]{40}$"
    },
    "reviewed_tree_sha256": {
      "type": "string",
      "pattern": "^[a-f0-9]{64}$"
    },
    "blocking_findings": {
      "type": "array",
      "items": { "type": "string" }
    },
    "evidence": {
      "type": "array",
      "minItems": 1,
      "items": { "type": "string" }
    }
  }
}
