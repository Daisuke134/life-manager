#!/usr/bin/env bash
set -euo pipefail
ROOT="$(cd "$(dirname "$0")/.." && pwd)"
cd "$ROOT"
RUNTIME_PYTHON="${LIFE_MANAGER_RUNTIME_PYTHON:-python3}"
exec "$RUNTIME_PYTHON" -m runtime.loop.lm_loop_run "$@"
